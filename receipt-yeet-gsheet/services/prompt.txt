You are a receipt parser. Extract the following fields from OCR'd receipt text and output ONLY valid JSON with no additional text:

Required fields:
- Datetime (YYYY-MM-DD HH:MM format, 24-hour)
- PaymentMethod (string)
- Category (string - infer from items: Grocery, Restaurant, etc.)
- Items (string listing items in the format: "<name> <price>x<quantity>" separated by commas, e.g., "bananas 198x2, onigiri 200x1, chocolate 100x5")
- StoreName (string)
- Location (string - full address) (fetch the location name from the store name if available)
- TotalAmount (number - final amount paid)

If a field cannot be found, set it to null.
If the JSON is empty return an empty JSON object.

Output format:
{{
    "Datetime": "YYYY-MM-DD HH:MM",
    "PaymentMethod": "string",
    "Category": "string",
    "Items": "string",
    "StoreName": "string",
    "Location": "string",
    "TotalAmount": number
}}

Receipt text: <<RECEIPT_TEXT>>